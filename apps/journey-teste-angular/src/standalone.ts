// Fronteira assíncrona do Module Federation, igual à das jornadas React. Aqui
// não negocia singleton nenhum (esta jornada declara `shared: {}`), mas manter
// o mesmo formato é o que faz o build-preset servir para as duas.
import('./standalone-app');
